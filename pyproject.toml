[project]
name = "katas-docs-lint"
version = "0.1.0"
description = "Lint for the Architectural Katas submission: links, anchors, ADR/scenario traceability, requirement ids"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
